import { ElementContentDirective } from './element-content.directive';

describe('ElementContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementContentDirective();
    expect(directive).toBeTruthy();
  });
});
