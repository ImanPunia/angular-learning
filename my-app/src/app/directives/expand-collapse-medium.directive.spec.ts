import { ExpandCollapseMediumDirective } from './expand-collapse-medium.directive';

describe('ExpandCollapseMediumDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandCollapseMediumDirective();
    expect(directive).toBeTruthy();
  });
});
